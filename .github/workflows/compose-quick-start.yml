name: Distribution quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  packages: read

jobs:
  release-quality:
    uses: ./.github/workflows/release-quality.yml
    with:
      release: false
