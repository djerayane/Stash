/** Portable fields owned by the knowledge-authoring capability and composed into legacy Note projections. */
export interface NoteTreeLocationFields {
  parentId?: string;
  position?: string;
  archivedAt?: string;
  trashedAt?: string;
}

export interface SemanticRelationshipFields {
  relationshipType?: string;
}
