# Share the server core with an optional desktop app

An optional desktop application bundles the same server core and domain model used by self-hosted Instances rather than creating a separate local-only product. This lets a solo Member migrate the complete local Instance to a server without semantic loss, while desktop-to-mobile connectivity remains a later convenience rather than a first-release requirement.
