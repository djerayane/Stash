# Preserve Note sources when creating Tasks

Members may create a Task directly in any Task View Block or deliberately create and link one from selected Note content. The source Block remains authored knowledge and visibly retains its Task relationship rather than being removed or silently converted; ordinary checklist items remain lightweight content unless explicitly promoted.
