# Expose MCP through the domain API

Every Instance includes a disabled-by-default MCP endpoint backed by the same permission-aware service layer as its public API, plus a thin local connector for clients that require a local process. Instance Administrators explicitly enable MCP and Members pair clients through Agent Grants rather than sharing account credentials; versioned open-source official skills build on this capability contract without receiving direct database access.
