# Expose agent access before building integrated AI

Stash first provides permission-aware APIs, events, an MCP server, and official agent guidance so user-chosen AI agents can operate against the same authorized domain capabilities without direct database access. A built-in AI model or agent is deferred, keeping model providers optional and self-hosting intact while preserving a path for agents to read context, propose organization, and perform approved changes.
