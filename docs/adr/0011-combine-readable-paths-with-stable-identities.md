# Combine readable paths with stable identities

Links use ordinary relative Markdown paths so exported content remains understandable, supplemented by documented stable identities so Stash can preserve relationships across renames and moves. Stash repairs paths only when the identity match is unambiguous; otherwise it presents an explicit broken-link workflow instead of guessing.
