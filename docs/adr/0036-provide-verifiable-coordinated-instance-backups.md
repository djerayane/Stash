# Provide verifiable coordinated Instance Backups

Stash supplies a backup command that coordinates database, Attachment, configuration, and encryption-key requirements into a versioned manifest with checksums and explicit secret-restoration status. It supports dry-run restore verification and surfaces backup age and verification health to Instance Administrators, while operators retain responsibility for schedules and off-site storage.
