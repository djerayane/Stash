# Disable telemetry by default

Instances make no diagnostic submissions unless an Instance Administrator explicitly opts in to a published, content-free schema whose pending payloads can be inspected. Crash reports remain locally downloadable unless separately enabled, and update checks are independently configurable so self-hosting does not imply undisclosed network traffic.
