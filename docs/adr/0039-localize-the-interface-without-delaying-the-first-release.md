# Localize the interface without delaying the first release

Stash ships English first but uses localization-ready message catalogs throughout, stores timestamps in UTC, and renders locale, time zone, date format, and week-start preferences per Member. Community translations can therefore arrive without redesigning the interface, while translation completeness does not block the initial product slice.
