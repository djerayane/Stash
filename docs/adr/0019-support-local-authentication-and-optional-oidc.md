# Support local authentication and optional OpenID Connect

Every Instance supports built-in accounts with passwords, passkeys, recovery codes, and optional email recovery when SMTP exists, so authentication never depends on a proprietary service. OpenID Connect provides optional organizational identity integration, open registration is disabled by default, and SAML is deferred until demonstrated demand justifies its additional complexity.
