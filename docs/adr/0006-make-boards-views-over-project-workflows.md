# Make boards views over Project Workflows

Each Project owns one configurable Workflow and its Tasks retain one canonical status. Boards are filtered and grouped views over those Tasks, so a Task may appear on several boards without being copied; moving it through status-backed columns changes the same status everywhere, while other boards may group by properties such as assignee, label, or priority.
