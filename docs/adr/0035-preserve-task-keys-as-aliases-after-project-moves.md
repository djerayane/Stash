# Preserve Task Keys as aliases after Project moves

A Task moved between Projects receives the destination's next Task Key while its previous keys remain permanently reserved aliases. Existing URLs, branches, commits, and textual references continue resolving to the same Task, the move appears in Activity, and retired keys are never reused for different work.
