# Make accessibility a stable release requirement

Core web flows target WCAG 2.2 AA with complete keyboard operation, visible focus, screen-reader semantics, sufficient contrast, reduced motion, accessible rich-text controls, and non-drag board alternatives; mobile clients use native accessibility APIs. Accessibility defects that prevent these core workflows block a stable release rather than being deferred as cosmetic follow-up.
