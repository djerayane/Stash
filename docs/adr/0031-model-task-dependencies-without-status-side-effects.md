# Model Task Dependencies without status side effects

Tasks may have directed Depends on and Required by relationships, with cycles rejected. A Dependency can produce warnings and drive Automations but never changes status automatically or prevents ordinary editing and progress, preserving human control while making blocked work visible; the interface avoids the word “Block” because Block already identifies content within a Note.
