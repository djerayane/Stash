# Store native Tasks as readable files

Every native Task has an individually readable Markdown file with documented structured metadata and a Markdown description. Relationships and external-provider references remain reconstructible from canonical Workspace files; Stash may maintain generated indexes for performance, but they are disposable and must never become the only representation of Task data.
