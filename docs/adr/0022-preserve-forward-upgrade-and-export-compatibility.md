# Preserve forward upgrade and export compatibility

Stash supports database migration from every supported release, imports every published Portable Workspace Export version, and documents a rolling compatibility window between mobile clients and servers. Upgrade preflight checks fail safely when requirements are unmet and protect recoverability rather than asking self-hosters to reinstall or abandon old data.
